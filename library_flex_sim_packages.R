# library_flex_packages.R 
# library() packages needed for flex bootstrap analyses

library_flex_sim_packages <- function(libdir) {
  library(RLT,lib.loc=libdir)
  library(DTRlearn,lib.loc=libdir)
}
